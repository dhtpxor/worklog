# 업무 비서 설치 안내 (개발자가 아니어도 따라 할 수 있게)

> 총 소요 시간: 기본 30분. 메일 5분, AI 10분은 나중에 해도 됩니다.
> 꼭 필요한 것: Google 계정, GitHub 계정, Vercel 계정(무료). 셋 다 무료입니다.
> 없어도 되는 것: 네이버웍스 관리자 권한, 회사 메일 외부 연동 권한. 둘 다 없어도 앱은 제대로 돕니다.

## 0. 전체 그림

```
 받은편지함 (IMAP, 선택) ──┐
 카톡·메신저 캡처 / 복사 ───┼──▶  Vercel 서버  ──▶  구글 시트 (할 일·일정·팀원)
 웹페이지 '대화' 탭 ───────┘         │      ▲
                                     ▼      │ 매시간 깨워줌
                             알림 메일 (SMTP, 선택)   Apps Script 트리거
                       아침 브리핑 · 일일보고 초안 · 팀원 리마인드
```

- **구글 시트** 한 장이 데이터베이스입니다. 시트를 열어 직접 고쳐도 됩니다.
- **웹페이지**(폰/PC)의 **대화** 탭에서 채팅하듯 할 일을 정리합니다. `정리`라고 말을 걸면 순서대로 물어봅니다.
- **캡처 이미지**(카톡·메신저·메일 화면)를 올리면 글자를 읽어 할 일 후보를 뽑습니다. AI 키가 없으면 브라우저 안에서만 읽어 이미지가 밖으로 나가지 않습니다.
- **메일(선택)** 은 두 가지로 씁니다. 받은 메일에서 할 일 후보를 뽑고(IMAP), 브리핑과 리마인드를 보냅니다(SMTP). 회사 메일이 막혀 있으면 보내기만 개인 메일로 해도 되고, 둘 다 안 써도 웹페이지로 다 됩니다.
- **AI 비서(선택)**: Gemini·Claude 키를 넣으면 자연어로 대화하며 할 일·일정을 처리하고, 메일·캡처에서 할 일을 훨씬 정확하게 뽑습니다. 키가 없으면 규칙(키워드·날짜 패턴)만 쓰고 아무것도 외부로 보내지 않습니다.
- **나만의 달력**: 구글 시트에 저장되는 자체 달력. 오늘·내일 일정이 브리핑에 들어가고, 달력 탭이나 대화로 일정을 관리합니다. 폰 캘린더에서 구독도 됩니다.

---

## 1. 구글 시트 + Apps Script (15분)

1. https://sheets.google.com 에서 빈 시트를 만들고 제목을 **업무 비서 데이터**로 바꿉니다.
2. 메뉴 **확장 프로그램 → Apps Script**. 기본 코드를 모두 지우고 이 저장소의 `apps-script/Code.gs` 전체를 붙여넣습니다.
   (GitHub 에서 파일을 열고 **Raw** 버튼 → 전체 선택 → 복사)
3. 맨 위 `API_TOKEN = "여기를-긴-비밀문자열로-바꾸세요"` 의 따옴표 안을 아무 긴 문자열로 바꿉니다. **메모해 두세요** → Vercel 의 `GAS_TOKEN`.
4. 저장 → 함수 선택 **setup** → ▶ 실행 → 권한 승인. 시트에 `tasks` `team` `reports` `state` `log` 탭이 생깁니다.
5. **배포 → 새 배포** → 톱니바퀴 → **웹 앱** → 실행 사용자 **나**, 액세스 **모든 사용자** → 배포 → **웹 앱 URL** 복사 → Vercel 의 `GAS_URL`.

> Code.gs 를 나중에 고치면 **배포 → 배포 관리 → 연필 → 버전: 새 버전 → 배포**. URL 은 그대로입니다.

## 2. Vercel 배포 (15분)

1. https://vercel.com → **Add New… → Project** → GitHub 의 `worklog` 저장소 Import.
2. **Framework Preset**: **Other**. Build Command 와 Output Directory 는 **비워 둡니다** (이 앱은 빌드가 없습니다). Root Directory 는 건드리지 않습니다.
3. **Environment Variables** — Key 칸에 이름, Value 칸에 값:

   | Key | Value |
   |---|---|
   | `GAS_URL` | 1-5 의 웹 앱 URL |
   | `GAS_TOKEN` | 1-3 에서 정한 문자열 |
   | `OWNER_NAME` | 내 이름 (카톡·메신저에 표시되는 이름과 같게) |
   | `ADMIN_PIN` | 웹페이지 로그인 숫자 |
   | `TICK_KEY` | 아무 긴 문자열 (5단계에서 다시 씀) |

   메일·AI·달력은 나중에 켜도 됩니다. 4장(메일)과 6장(AI)에서 안내합니다.
4. **Deploy** → 주소(예: `https://worklog-xxx.vercel.app`)를 메모.
5. 주소를 열고 PIN 입력 → **설정 점검** 탭 → "연결 상태 확인" → `sheet: ok: true` 면 성공. 폰에서도 같은 주소로 들어갑니다 (홈 화면에 추가해 두면 앱처럼 씁니다).

> 환경변수를 바꾸면 **Deployments → 최신 항목 ⋯ → Redeploy** 를 눌러야 반영됩니다.

## 3. 바로 써 보기 (5분)

웹페이지 **대화** 탭에서:

- `정리` → 봇이 "할 일을 적어 주세요" → 한 줄에 하나씩 적기 (예: `A사 견적서 보내기 ~내일`) → `끝` → 기한 없는 것을 하나씩 물어봄
- `할일 회의실 예약 ~금요일` / `지시 김철수 주간보고 취합 ~내일` / `완료 3` / `목록` / `브리핑` / `보고`
- 아무 말이나 적어도 됩니다. "영희님 회의록 목요일까지 공유해주세요" → 이영희 업무로 등록.

**가져오기** 탭: 카톡·네이버웍스 화면 캡처를 올리거나(여러 장 가능), 대화를 복사해 붙여넣고 **미리보기** → **후보로 저장** → **후보** 탭에서 확정/무시.

## 4. 메일 연결 (선택, 5분)

메일은 **받기**와 **보내기**가 서로 다른 계정이어도 됩니다. 회사 메일이 외부 연동을 막아 두었다면 받기는 포기하고, 보내기만 개인 Gmail·네이버 메일로 하면 됩니다. 둘 다 안 해도 앱은 그대로 돌아갑니다. 웹페이지에서 직접 보면 되니까요.

| | 하는 일 | 없으면 |
|---|---|---|
| **받기 (IMAP)** | 받은편지함에서 할 일 후보를 뽑고, 팀원이 보낸 일일보고 답장을 저장 | 가져오기 탭에 복사·캡처로 넣으면 됨 |
| **보내기 (SMTP)** | 아침 브리핑·저녁 일일보고 초안을 내게, 리마인드·보고 요청을 팀원에게 | 알림이 안 옴. 웹페이지를 직접 열어서 확인 |

### 서버 주소

| 서비스 | 받기(IMAP) | 보내기(SMTP) | 비밀번호 |
|---|---|---|---|
| 메일플러그 | `imap.mailplug.co.kr` / 993 | `smtp.mailplug.co.kr` / 465 | 메일 비밀번호 (관리자가 IMAP 을 허용해야 함) |
| Gmail | `imap.gmail.com` / 993 | `smtp.gmail.com` / 465 | **앱 비밀번호** (2단계 인증 필수) |
| 네이버 | `imap.naver.com` / 993 | `smtp.naver.com` / 465 | **앱 비밀번호** (2단계 인증 시) |

### 넣을 환경변수

받기와 보내기를 따로 씁니다. 안 쓰는 쪽은 비워 두세요.

```
IMAP_HOST=imap.mailplug.co.kr
IMAP_PORT=993
IMAP_USER=hong@company.co.kr
IMAP_PASS=········

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=hong@gmail.com
SMTP_PASS=········          # Gmail 앱 비밀번호 16자리
OWNER_EMAIL=hong@company.co.kr   # 브리핑을 받을 주소. 비우면 SMTP_USER 로 옴
```

넣고 나서 **Redeploy** → **설정** 탭에서 `mail: ok`(받기)와 `smtp: ok`(보내기)를 확인하세요.

### 서비스별 준비

- **메일플러그**: 웹메일 → 환경설정(톱니바퀴) → 메일 → POP3/IMAP 설정 → **IMAP 사용**. 항목이 잠겨 있거나 아예 없으면 사내 관리자만 풀 수 있습니다. 2025년 4월 이후 개통 계정은 기본이 "사용 안 함"입니다. ([매뉴얼](https://mailplug-help.atlassian.net/wiki/spaces/MM/pages/7602243)) 관리자가 안 열어 주면 받기는 포기하고, 필요한 메일만 복사해 **가져오기** 탭에 넣으세요.
- **Gmail**: 일반 비밀번호로는 안 됩니다. Google 계정 → 보안 → **2단계 인증**을 켠 뒤 **앱 비밀번호**를 만들어 그 16자리를 넣으세요. ([설정 안내](https://knowledge.workspace.google.com/admin/sync/set-up-gmail-with-a-third-party-email-client?hl=ko))
- **네이버**: 메일 → 환경설정 → **POP3/IMAP 설정** → IMAP/SMTP **사용함**. 2단계 인증을 쓰면 네이버ID → 보안 설정 → 2단계 인증 → **애플리케이션 비밀번호**를 만들어 넣습니다. ([설정 안내](https://comeinsidebox.com/naver-mail-imap-pop3/))

받기를 켜면 매시간 새 메일에서 후보를 뽑습니다. 메일을 **읽음 처리하지 않고**, 업무 비서가 보낸 `[업무비서]` 메일은 후보로 잡지 않습니다.

> 회사 메일을 개인 메일로 **자동 전달**할 수 있다면 또 다른 방법이 있습니다. 메일플러그에서 전달 규칙을 만들어 Gmail 로 보내고, 받기를 그 Gmail 에 걸면 회사 IMAP 없이도 수집이 됩니다. 사내 보안 정책상 전달이 허용되는지는 먼저 확인하세요.

## 5. 매시간 깨우는 트리거 (5분)

Vercel 무료 플랜의 자체 예약 실행은 하루 1번(아침 8시 브리핑)뿐이라, 구글 Apps Script 가 매시간 서버를 깨워 메일 수집·리마인드·저녁 초안을 돌립니다.

1. Apps Script 편집기 맨 위 `TICK_URL = ""` 에 `https://<Vercel 주소>/api/tick?key=<TICK_KEY 값>` 을 넣고 저장.
2. 함수 선택 **installTriggers** → ▶ 실행. 로그에 `매시간 tick 트리거 설치됨`.
3. 확인: 브라우저에서 `https://<Vercel 주소>/api/tick?key=<TICK_KEY>&force=brief` 를 열면 지금 당장 브리핑 메일이 옵니다.

## 6. AI 비서 켜기 (10분) — Gemini + Claude 자동 배분

키를 넣으면 **대화** 탭이 진짜 비서가 됩니다. "내일 3시 A사 미팅 잡아줘", "이번 주 뭐 해야 하지?", "김철수한테 견적서 금요일까지 시켜줘", "오늘 일정 알려줘" 같은 말을 알아듣고, 할 일·일정을 실제로 등록·수정한 뒤 결과를 알려줍니다. 메일·캡처에서 할 일을 뽑는 것도 AI가 하므로 정확도가 크게 올라갑니다.

**자동 배분**: 짧은 대화·단순 명령·메일 추출·캡처 읽기는 Gemini(저렴, 무료 구간 있음), 분석·보고서/메일 초안 작성·긴 글은 Claude Opus 5. 한쪽이 장애면 다른 쪽으로 넘어갑니다. `AI_BUDGET_KRW`(기본 1만 원)를 넘으면 그 달은 Gemini만 씁니다. 사용액 추정은 **설정 점검** 탭 `aiSpend`에서 봅니다 (환율·요금표 기준 추정치이지 청구서가 아닙니다).

### ⚠️ 쓰고 계신 유료 구독으로는 안 됩니다

이 부분을 가장 많이 헷갈립니다. **구독과 API 는 완전히 다른 상품**입니다.

| | 무엇에 쓰나 | 이 앱에서 |
|---|---|---|
| Claude Pro·Max 구독 | claude.ai 웹·앱, Claude Code | **못 씀** |
| Gemini Advanced (Google One AI) 구독 | Gemini 앱·웹 | **못 씀** |
| **API 키** | 프로그램이 직접 호출 | **이것이 필요함** |

이 앱은 내 Vercel 서버가 프로그램으로 직접 부르기 때문에 API 키가 있어야 합니다. 구독료를 이미 내고 있어도 API 사용료는 **따로** 청구됩니다. 반대로 API 키만 있으면 구독은 없어도 됩니다.

### 키 받기

1. **Gemini 키** — https://aistudio.google.com → 로그인 → **Get API key** → **Create API key** → 복사 → Vercel `GEMINI_API_KEY`.
   - Flash 계열은 **무료 구간**이 있습니다. 분당·하루 요청 수 제한 안에서 쓰면 돈이 안 듭니다. 이 앱의 평소 사용량이면 무료 구간으로 충분할 가능성이 높습니다. 정확한 한도와 요금은 Google 요금 페이지에서 확인하세요.
2. **Claude 키** — https://console.anthropic.com → 가입 → **Billing** 에서 결제 수단 등록(선불 크레딧) → **API Keys → Create Key** → 복사 → Vercel `ANTHROPIC_API_KEY`.
   - 무료 구간이 없습니다. 최소 금액을 충전해 두고 쓰는 방식입니다.
   - 요금은 Opus 5 기준 입력 100만 토큰당 $5, 출력 $25. 이 앱은 분석·작성 같은 어려운 요청에만 Claude 를 쓰므로 개인 사용이면 보통 월 수천 원 안쪽이지만, 쓰기 나름입니다.
3. Vercel **Redeploy** → **설정** 탭에서 `gemini: ok`, `claude: ok` 확인.

> **키 하나만 넣어도 됩니다.** Gemini 만 넣으면 전부 Gemini 가, Claude 만 넣으면 전부 Claude 가 처리합니다. 돈이 걱정되면 Gemini 부터 시작하세요.
> 키를 넣는 순간부터 메일 본문·캡처 이미지·대화 내용이 Google·Anthropic 서버로 전송됩니다. 회사 보안 정책을 먼저 확인하세요.
> 아무 키도 안 넣으면 규칙 기반으로만 돕니다. 정확도는 떨어지지만 아무것도 외부로 나가지 않고 돈도 안 듭니다.

## 7. 나만의 달력

별도 계정이 필요 없습니다. 일정은 구글 시트 `events` 탭에 저장됩니다.

- **달력** 탭: 월간 보기. 날짜를 누르면 그 날 일정이 아래에 나오고, 제목·시각(예: `15:00`, `오후 3시`, 비우면 종일)·장소를 적어 추가합니다. 수정·삭제도 거기서.
- **대화** 탭(AI 켠 경우): "내일 3시 A사 미팅 잡아줘", "이번 주 일정 뭐 있어?", "A사 미팅 4시로 옮겨줘", "그 일정 지워줘"처럼 말하면 됩니다.
- **아침 브리핑**과 **오늘** 탭에 오늘·내일 일정이 나옵니다.
- **폰 기본 캘린더에서 보기**: 설정 점검의 `calendar.icalUrl` 주소(`https://<Vercel 주소>/api/ical?key=<TICK_KEY>`)를 아이폰 "캘린더 → 계정 추가 → 기타 → 구독 캘린더 추가", 안드로이드는 구글 캘린더 웹 "다른 캘린더 추가 → URL로 추가"에 넣으면 됩니다. 구독은 읽기 전용이고 갱신 주기는 캘린더 앱이 정합니다(몇 시간~하루).

> Code.gs 를 예전에 붙여넣었다면 최신 파일로 다시 붙여넣고 **setup 을 한 번 더 실행**해야 `events` 탭이 생깁니다. 기존 데이터는 그대로 유지됩니다.

## 8. 팀원

- **팀원** 탭에서 이름 + **이메일**을 등록하거나, 대화 탭에서 `팀원등록 김철수 kim@company.co.kr 대리`.
- `지시 김철수 …` 하면 김철수에게 "새 업무" 메일이 갑니다.
- 매일 9시 기한 임박·지연 리마인드 메일, 17시 "오늘 한 일을 답장해 주세요" 메일이 팀원에게 갑니다. 팀원이 **제목 그대로 답장**하면 다음 메일 수집 때 자동으로 보고로 저장되고, 18시 일일보고 초안과 다음 날 아침 브리핑에 정리돼 나옵니다.
- 팀원별로 일일보고 요청을 끌 수 있습니다 (팀원 탭 "일일보고 끄기").

## 9. 하루 흐름

| 시각(평일) | 무슨 일이 | 내가 할 일 |
|---|---|---|
| 08시 | **아침 브리핑** 메일: 지연·오늘 마감·이번 주·팀원 업무·확인 필요 후보·어제 팀 보고 | 후보는 웹에서 확정/무시, 또는 대화 탭에서 `정리` |
| 09시 | 팀원에게 리마인드 메일 | 없음 |
| 매시간 | 새 메일 → 후보, 팀원 답장 → 보고 | 없음 |
| 17시 | 팀원에게 "오늘 한 일" 요청 메일 | 없음 |
| 18시 | **일일업무보고 초안** 메일 (금일 완료·진행·내일 예정·팀원 보고) | 복사해서 보고 형식에 붙임 |
| 수시 | 대화 탭·캡처 가져오기·체크 | |

시각은 `BRIEF_HOUR`, `REMIND_HOUR`, `ASK_HOUR`, `EVENING_HOUR`(24시간제), `WORKDAYS`(0=일…6=토) 환경변수로 바꿉니다. 트리거가 "매시간 임의의 분"에 돌아 최대 1시간 가까이 늦을 수 있습니다.

### 대화 탭 명령

| 보내는 말 | 결과 |
|---|---|
| `정리` | 대화형 정리 (적기 → `끝` → 기한 하나씩 질문) |
| `할일 내용 ~날짜` | 내 할 일. `~` 뒤에 내일/모레/금요일/9월30일/9/30/다음 주 월요일/월말 |
| `지시 이름 내용 ~날짜` | 팀원 업무 + 알림 메일 |
| `목록` `오늘` `팀` `후보` `보류목록` | 보기 |
| `완료 12` `보류 12` `재개 12` `삭제 12` `확정 12` `무시 12` | 상태 (여러 개: `완료 12 13`) |
| `미룸 12 다음 주 월요일` `담당 12 김철수` `제목 12 새 제목` | 수정 |
| `브리핑` `보고` | 지금 바로 브리핑 / 일일보고 초안 |
| `팀원등록 이름 이메일 직급` `팀원삭제 이름` | 팀원 |
| 그 밖의 아무 말 | 메모로 보고 할 일 추출 |

## 10. 문제 해결

- **설정 점검** 탭이 첫 번째 진단 도구입니다.
- 달력 탭이 "event 없음"/"unknown action" 오류 → Code.gs 가 옛 버전입니다. 최신 Code.gs 로 바꾸고 setup 실행 → 새 버전으로 배포.
- 브리핑 메일이 안 옴 → `smtp: ok` 인지, 5장 트리거가 설치됐는지(Apps Script 왼쪽 시계 모양 "트리거"), 오늘이 `WORKDAYS` 인지. 스팸함도 확인.
- 메일 연결 실패 → 오류 문구를 보세요. `Invalid credentials` / `AUTHENTICATIONFAILED` 는 비밀번호 문제입니다. Gmail·네이버는 **앱 비밀번호**여야 합니다. 회사 메일은 관리자가 IMAP 을 막아 둔 경우가 많고, 그때는 받기를 비우고 보내기만 개인 메일로 쓰면 됩니다 (4장).
- AI 가 안 붙음 → 구독(Claude Pro·Gemini Advanced)으로는 안 됩니다. API 키를 따로 발급해야 합니다 (6장). Claude 는 크레딧을 충전해 두어야 `claude: ok` 가 뜹니다.
- 캡처 글자 인식이 엉망 → 캡처를 크게(글자가 또렷하게) 찍거나, 텍스트를 복사해 붙여넣으세요.
- 엉뚱한 후보가 많음 → 정상입니다. 규칙 단어는 `lib/extract.js` 상단 `REQUEST_CUES` 등에서 넣고 뺄 수 있습니다.
- 시트 `log` 탭에 오류가 기록됩니다.

## 11. 데이터는 어디에 있나 (보안)

- 할 일·팀원·보고: **내 구글 시트**에만. 메일·메시지 본문은 200자 조각(snippet)만 남고 원문은 저장하지 않습니다.
- 캡처 이미지: 브라우저 안에서만 글자를 읽고 서버로 보내지 않습니다. 읽어낸 글은 후보 추출을 위해 내 Vercel 서버로 갑니다.
- AI 키를 넣지 않으면 외부 AI 서비스로 아무것도 보내지 않습니다. 키를 넣으면 대화·메일 본문·캡처 이미지가 Google(Gemini)·Anthropic(Claude)으로 전송됩니다.
- 비밀값은 Vercel 환경변수에만. 저장소에는 절대 넣지 마세요 (`.env` 는 `.gitignore` 에 들어 있습니다).

---

## 부록 A. 네이버웍스 봇 (관리자 권한이 생기면)

봇을 붙이면 웹페이지 대신 네이버웍스 1:1 방에서 같은 명령을 쓰고, 봇을 팀 그룹방에 초대해 대화를 자동 수집하며, 알림이 메일 대신(또는 함께) 봇 메시지로 옵니다. 필요한 값: 앱의 Client ID / Client Secret / Service Account / Private Key(scope `bot`), 봇의 Bot ID / Bot Secret, Callback URL `https://<Vercel 주소>/api/callback`. 공식 문서: https://developers.worksmobile.com/kr/docs/bot , https://developers.worksmobile.com/kr/docs/auth-jwt . 값을 `WORKS_*` 환경변수에 넣고 Redeploy 한 뒤, 봇 1:1 방에서 `관리자 <ADMIN_PIN>` 을 보내면 연결됩니다. 팀원은 봇에 `등록 이름`.
